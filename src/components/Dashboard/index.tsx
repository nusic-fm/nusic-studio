import { Container } from "@mui/system";
import Overview from "../Overview";

const Dashboard = () => {
  return (
    <Container sx={{ p: 4 }}>
      <Overview />
    </Container>
  );
};

export default Dashboard;
